"""Leaf utilities: HTTP sessions, timestamps, the prospect day, run metrics.

Nothing here imports another sub-package, which is what lets storage-agnostic
modules use these without pulling in the config/HTTP chain.
"""
