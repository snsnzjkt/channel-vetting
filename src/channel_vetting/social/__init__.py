"""
TikTok + Instagram creator sourcing for the Mythumi figurine business.

Separate from the YouTube path in `discovery/` and `enrichment/` because the
two platforms have no free statistics source: on YouTube every number comes
from the YouTube Data API for quota rather than money, while here the numbers
that decide whether a creator qualifies have to be bought from
influencers.club. Everything that is genuinely shared — the credit ledger and
its ceilings, DO NOT CONTACT screening, the Airtable client — is imported, not
reimplemented.
"""
