"""Is this channel actually about the niche?

Three tiers of evidence, cheapest first: `video_topics` reads topic data
already paid for, `transcripts` fetches the spoken text free, and `gemini`
is the tier that decides — and the only one that costs requests.
"""
