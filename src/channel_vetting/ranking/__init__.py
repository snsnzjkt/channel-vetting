"""Ordering, not gating — nothing here can drop a prospect.

`scoring` rates a single channel (fake-follower risk, weighted overall).
`keyword_history` orders the review queue by how the keyword that found a
channel has actually performed with the reviewer.
"""
