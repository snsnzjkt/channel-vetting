.env
search_cache.json
quota_log.json
external_handles_cache.json
venv/
.venv/
__pycache__/
*.pyc
.claude
CLAUDE.md
.superpowers
\superpowers
docs

# cloakbrowser-mcp is run via `npx` (see .mcp.json); these should never
# appear, but keep them out if a local npm install is ever done instead.
node_modules/
package-lock.json
.gstack/
